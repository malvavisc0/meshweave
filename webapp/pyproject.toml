[build-system]
requires = ["setuptools>=82", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "meshweave-webapp"
version = "0.1.9"
description = "Turn websites into words and signals"
requires-python = ">=3.14"
readme = ""
authors = [{ name = "MeshWeave", email = "hello@meshweaveai.com" }]
dependencies = []

[project.optional-dependencies]
runtime = [
  "fastapi[standard]",
  "uvicorn[standard]",
  "jinja2",
  "sqlalchemy",
  "psycopg2-binary",
  "python-multipart",
  "httpx",
  "authlib",
  "cryptography",
  "prometheus-client",
  "alembic",
  "openai",
  "sentry-sdk",
  "langfuse",
]

[tool.setuptools]
packages = ["webapp", "webapp.routers", "webapp.services", "webapp.utils"]
package-dir = { "webapp" = "." }

[tool.setuptools.package-data]
webapp = [
  "templates/*.html",
  "static/*",
  "static/**/*.css",
  "static/**/*.js",
  "static/**/*.map",
  "static/**/*.*",
]

[project.scripts]
meshweave = "webapp.cli:main"
